int main() {
    int x = 10;
    int y = 20;
    
    // This will generate a temp variable (e.g., t0) in the IR
    int z = x + y; 

    // This uses a literal (should NOT be in .data)
    if (z > 5) {
        cout << z;
    }
}